import React from "react";
import "./App.css";
import { FlexContainer, SeasonalityChart } from "components";

const App = (props) => {
  return (
    <FlexContainer>
      <SeasonalityChart />
    </FlexContainer>
  );
};

export default App;
